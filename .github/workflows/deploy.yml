name: Build

on:
  push:
    branches: [master]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci
      - run: npm run build

      - name: Set version & date
        id: meta
        run: |
          v=$(date -u '+%Y-%j-%H')
          d=$(date -u '+%Y-%m-%d %H:%M UTC')
          echo "version=$v" >> $GITHUB_OUTPUT
          echo "datetime=$d" >> $GITHUB_OUTPUT

      - name: Create tarball
        run: (cd _site && shopt -s dotglob && tar -czf ../site.tar.gz *)

      - name: Generate checksum
        run: sha256sum site.tar.gz > site.tar.gz.sha256

      - name: Debug tarball contents
        run: tar -tzf site.tar.gz | head -20

      - name: Verify output
        run: ls -lh _site/index.html _site/actualites/index.html site.tar.gz

      - uses: actions/upload-artifact@v4
        with:
          name: site-${{ steps.meta.outputs.version }}
          path: site.tar.gz
          compression-level: 0

      - name: Publish release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release delete latest --yes --cleanup-tag 2>/dev/null || true
          gh release create latest site.tar.gz site.tar.gz.sha256 \
            --title "${{ steps.meta.outputs.version }}" \
            --notes "Build ${{ steps.meta.outputs.version }} — ${{ steps.meta.outputs.datetime }}

          ### Download &amp; verify
          \`\`\`
          curl -LO ${{ github.server_url }}/${{ github.repository }}/releases/download/latest/site.tar.gz
          curl -LO ${{ github.server_url }}/${{ github.repository }}/releases/download/latest/site.tar.gz.sha256
          sha256sum -c site.tar.gz.sha256
          \`\`\`
          Do **not** use the Source code archive — use the attached **site.tar.gz** asset."
